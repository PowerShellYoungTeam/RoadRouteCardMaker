# Web App (React)

This folder will contain a simple React web application for creating and printing truck route cards.

## Setup

1. Open a terminal in this folder.
2. Run `npx create-react-app .` to initialize the React app.
3. Follow the README in the root for more details.
