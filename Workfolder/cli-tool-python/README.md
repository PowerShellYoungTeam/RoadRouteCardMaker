# Command-Line Tool (Python)

This folder will contain a Python script to generate route cards from the terminal.

## Setup

1. Open a terminal in this folder.
2. Run `python route_card.py` to use the tool (script will be provided).
3. See the README in the root for more details.
