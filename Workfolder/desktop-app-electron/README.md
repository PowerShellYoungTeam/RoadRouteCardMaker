# Desktop App (Electron)

This folder will contain an Electron-based desktop application for offline route card creation.

## Setup

1. Open a terminal in this folder.
2. Run `npm init -y` to initialize a Node.js project.
3. Install Electron with `npm install electron --save-dev`.
4. See the README in the root for more details.
